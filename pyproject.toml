[project]
name = "h3-migration"
version = "0.1.0"
description = "Migrate walkthru-earth S3 Parquet files to optimized H3 int64 schema"
requires-python = ">=3.12"
dependencies = ["boto3>=1.42.61", "duckdb==1.5.0", "pyarrow>=23.0.1"]
